import { Given,When,Then } from "playwright-bdd/decorators";

Given('user navigate to ([^"]+)')
async function navigateToWebsite(url:string) {
    
}
  
  When('user click on ([^"]+) button')
  async function clickButton(element:string) {
    
  }
  Then('check if ([^"]+) element has text {string}')
  async function name(elementName:string,txt:string) {
    
  }
 